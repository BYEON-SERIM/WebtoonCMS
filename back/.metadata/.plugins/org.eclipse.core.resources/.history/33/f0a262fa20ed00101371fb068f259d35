package com.example.cms.config;

import com.example.cms.domain.Webtoon;
import org.springframework.data.jpa.repository.JpaRepository;

// JpaRepository<엔티티이름, ID타입>을 상속받으면
// findAll(), save(), deleteById() 등을 공짜로 쓸 수 있습니다.
public interface DataInitializer extends JpaRepository<Webtoon, Long> {
}